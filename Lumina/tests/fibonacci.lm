# Teste de recursão e matemática
fn fib(n: int) -> int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

fn main() -> int:
    print("Sequência de Fibonacci:")
    for i in 0..10:
        print(fib(i))
    return 0
